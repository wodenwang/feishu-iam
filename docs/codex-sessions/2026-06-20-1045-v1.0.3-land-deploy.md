# v1.0.3 远端部署会话归档

## 会话目标

继续推进 `my-harness`，完成 `v1.0.3 - Base Portal 接入包与完整提示词收敛` 的收尾和远端部署。

## 用户关键要求

- 执行到收尾和远端部署为止。
- 保持 `my-harness` 15 步证据账本。
- 不把并行域名清理改动混入 `v1.0.3` 发布和部署。
- 不记录明文 secret、token、cookie、密码或 `.env` 敏感值。

## 关键决策

- 使用已经发布的 `v1.0.3` tag / commit `53f94b01d65eb8fb77f76eddcb8e545bcb4f39db` 作为部署来源。
- 本地工作区存在并行域名清理改动，因此新增干净 worktree `/tmp/feishu-iam-v1.0.3-clean` 构建镜像，避免污染部署产物。
- 沿用 `v1.0.2` 的单机 Docker Compose 停机升级模型：构建 linux/amd64 离线镜像，传输到 `bpmt@120.24.236.92`，远端 `FEISHU_IAM_PULL_POLICY=never`。

## 修改过的文件

- `README.md`
- `CHANGELOG.md`
- `docs/acceptance/v1.0.3-base-portal-integration-prompt.md`
- `.my-harness/status.md`
- `.my-harness/runs/2026-06-20-v1.0.3-land-deploy.md`
- `docs/codex-sessions/2026-06-20-1045-v1.0.3-land-deploy.md`

## 关键命令和验证结果

- `git worktree add --detach /tmp/feishu-iam-v1.0.3-clean v1.0.3`：创建干净部署 worktree，HEAD 为 `53f94b0`。
- `docker buildx build --platform linux/amd64 -f deploy/api.Dockerfile -t feishu-iam:v1.0.3 --load .`：通过，镜像平台为 linux/amd64。
- `docker image inspect feishu-iam:v1.0.3`：本地 manifest digest 为 `sha256:fad6c05532a12fbc4c83abc83e00a6d606eb222978e912c304a05e5740622c1c`。
- `docker save feishu-iam:v1.0.3 | gzip -1 | ssh ... 'gzip -dc | docker load'`：远端加载 `feishu-iam:v1.0.3` 成功。
- 远端 `.env` 只更新非敏感版本键：`FEISHU_IAM_IMAGE=feishu-iam`、`FEISHU_IAM_IMAGE_TAG=v1.0.3`、`APP_VERSION=1.0.3`、`GIT_COMMIT=53f94b0`、`FEISHU_IAM_PULL_POLICY=never`。
- `.env` 备份：`/home/bpmt/feishu-iam/backups/env-before-v1.0.3-20260620-104059`。
- `cd /home/bpmt/feishu-iam && ./upgrade.sh`：通过，升级备份目录为 `/home/bpmt/feishu-iam/backups/20260620-104106`。
- 远端容器：`feishu-iam-web-1 feishu-iam:v1.0.3 Up`，端口 `8002->3000`。
- 本机和公网 `/ready` 返回 ready。
- 本机和公网 `/version` 返回 `1.0.3 / 53f94b0`。
- 远端日志显示 `POST /api/v1/admin/applications/:appKey/integration-prompt/refresh` 路由已映射，Nest application successfully started。

## 未完成事项

- 并行域名清理改动仍保留在本地工作区，未纳入 `v1.0.3` 部署收尾提交。
- 可选后续：如需要更长时间的生产观察，可单独执行 `my-harness-canary`。
