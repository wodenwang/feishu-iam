# v0.1.12 Third-party OAuth Demo QA 记录

范围：OAuth runtime、Application API bearer token 权限查询、Demo OAuth mode、版本元数据和文档。

## 结论

通过。后端、前端构建、前端测试、OAuth server integration test、浏览器主路径和无权限路径均已验证。

## 命令验证

| 命令 | 结果 |
|---|---|
| `node --check examples/thirdparty-demo/src/server.js` | 通过 |
| `git diff --check` | 通过 |
| `grep -R "sec_\\|api_\\|app_secret" examples/thirdparty-demo docs/integration --exclude=.env.example || true` | 通过，无输出 |
| `npm run server:build` | 通过 |
| `npm run build` | 通过，有既有 chunk size warning |
| `npm test` | 通过，25 files / 116 tests |
| `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/oauth.test.ts` | 通过，3 tests |
| `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test` | 通过，17 files / 70 tests |

## 浏览器验证

使用 gstack browse：

1. 打开 `http://127.0.0.1:4200`，首页显示 OAuth 登录入口。
2. 以 `ou_demo_customer_allowed` IAM session cookie 进入 `/login`，完成 OAuth redirect 和 token exchange，最终到 `/customers`。
3. 以 `ou_demo_customer_denied` IAM session cookie 进入 `/login`，最终到 `/403`。

截图：

- `design/implementation-screenshots/v0.1.12-thirdparty-oauth-demo-home.png`
- `design/implementation-screenshots/v0.1.12-thirdparty-oauth-demo-customers.png`
- `design/implementation-screenshots/v0.1.12-thirdparty-oauth-demo-403.png`

## 发现与修复

1. 完整 `server:test` 首次失败：`server/tests/migrations.test.ts` 仍只期待 `001_runtime` 和 `002_access_loop`。
   - 处理：更新 migration test，纳入 `003_thirdparty_oauth` 和 OAuth expiry indexes。
   - 复验：完整 `server:test` 通过。
2. 代码审查发现 `authorize` invalid client/redirect 和 disabled application 失败未写审计。
   - 处理：补 `oauth.authorize` failure audit，并在 OAuth test 中断言 invalid redirect failure audit。
   - 复验：OAuth targeted test 和完整 `server:test` 通过。

## Review-loop metrics

- design-review: iterations run 1, findings discovered 0, findings fixed 0, accepted/deferred 0, remaining 0, final status cleared
- qa: iterations run 2, findings discovered 2, findings fixed 2, accepted/deferred 0, remaining 0, final status cleared
- review: iterations run 1, findings discovered 1, findings fixed 1, accepted/deferred 0, remaining 0, final status cleared
