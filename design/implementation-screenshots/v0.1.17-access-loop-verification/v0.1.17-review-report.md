# v0.1.17 接入闭环验收包代码审查记录

范围：当前 diff 中与 `v0.1.17` 第一个 vertical slice 相关的计划、脚本和文档改动。

## 结论

未发现阻塞问题。diff 保持在验收包边界内，没有新增 username/password、本地用户源、`/directory` 扩展、OAuth/OIDC 产品化、secret rotation 或同步增量能力。

## Review 循环

- 循环次数：1
- 发现问题：0 个阻塞问题
- 已修复：0 个
- 剩余问题：0
- 状态：cleared

## 重点检查

- `scripts/verify-v0.1-access-loop.sh`
  - 使用 HTTP API black-box 验收，不直接写数据库。
  - OAuth authorize 使用 manual redirect 读取 code，再通过 token endpoint 换取 bearer token。
  - 权限查询使用 bearer token + Application API HMAC。
  - 输出只包含 base URL、appKey、权限摘要和 sync health，不输出 `appSecret`、`apiSecret`、cookie、bearer token 或 HMAC signature。
  - sync 只检查 status/preflight，不触发真实 full sync 或增量同步。
- `README.md`
  - 新增 v0.1 接入闭环验收入口。
  - 区分自动 mock 验收、Demo 浏览器验收和真实飞书外部配置。
  - 未引入私有部署目标或真实凭证。
- `examples/thirdparty-demo/README.md` 与 `docs/integration/thirdparty-demo.md`
  - 明确 OAuth mode 是当前默认验收路径。
  - 保留 mock fallback 的非生产定位。
  - 明确 allow/deny 预期结果。
- `IMPLEMENTATION_PLAN.md` 与工程评审文档
  - 当前计划锁定第一 vertical slice，远端 release/push/deploy 作为授权后的 ship 与 land-and-deploy 收口动作执行。

## 风险与后续

- 当前脚本假设 runtime 使用 `FEISHU_AUTH_MODE=mock` 且本地回调地址为 `http://127.0.0.1:4200/oauth/callback`，这符合自动验收边界；真实飞书验收仍需要外部飞书配置。
- 本切片没有新增前端 UI 代码，因此未执行视觉设计 review。后续如果 release 收口需要截图，应按 README 启动 Demo 后补浏览器截图。
- push、PR、tag、GitHub Release 和 deploy 需要在验证通过后按授权进入最终收口。
