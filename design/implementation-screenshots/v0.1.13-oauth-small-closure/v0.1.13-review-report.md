# v0.1.13 OAuth 小收口代码 Review

日期：2026-05-26

## Review 范围

- OAuth authorize pending request 创建、恢复、消费和清理。
- Feishu mock login 与真实 Feishu callback 的 post-login redirect 优先级。
- DB migration、测试清理、文档和版本号。
- Feishu-only IAM 边界、第三方 OAuth demo runtime 边界和公开文档边界。

## 发现与处理

| 严重级别 | 发现 | 处理 |
|---|---|---|
| P1 | 未登录的 OAuth authorize 若不先校验 app / redirect，会允许无效 client 生成 pending request。 | 已修复：创建 pending 前先校验 `client_id + redirect_uri + active status`，无效请求返回 400 且不写 pending cookie。 |
| P3 | Mock login 中 `set-cookie` 先写 session cookie，pending restore 时再覆盖为数组，可读性差。 | 已整理为统一 cookie 数组写入。 |

## Review Loop

- 循环次数：1
- 发现问题：2
- 修复问题：2
- 遗留问题：0
- 最终状态：cleared

## 结论

当前 diff 保持在 v0.1.13 小切片范围内：只收口 OAuth 登录恢复、过期清理、审计和文档，不重新打开 `/directory`、同步、OIDC、refresh token 或 redirect URI 管理 UI。
