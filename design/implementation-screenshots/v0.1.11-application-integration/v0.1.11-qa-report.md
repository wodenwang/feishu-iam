# v0.1.11 应用接入配置 QA 记录

范围：Application Integration Runtime、README 重构、版本元数据。

## 结论

通过。v0.1.11 的后端、前端、构建和核心 HTTP mode E2E 均已验证。`npm run e2e` 在 HTTP mode 下会包含历史 mock-only `tests/e2e/access-loop.spec.ts`，该用例预期 `/dashboard`、`/sync` 等 mock 路由，与当前 HTTP route whitelist 不兼容；已单独用 mock mode 证明该历史用例仍通过。

## 验证命令

| 命令 | 结果 |
|---|---|
| `npm test -- src/features/iam/httpApi.test.ts src/pages/Applications/Detail.test.tsx src/pages/Applications/Onboarding.test.tsx src/router/routes.test.tsx` | 通过，4 files / 29 tests |
| `TEST_DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam_manual npm run server:test -- server/tests/applications.test.ts server/tests/audit.test.ts` | 通过，2 files / 18 tests |
| `TEST_DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam_manual npm run server:test` | 通过，16 files / 67 tests |
| `npm test` | 通过，25 files / 116 tests |
| `npm run build` | 通过，Vite chunk size warning only |
| `npm run server:build` | 通过 |
| `curl -fsS http://127.0.0.1:4100/api/health` | 通过，返回 `{"ok":true}` |
| `E2E_RESET_DATABASE=true TEST_DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam_manual VITE_IAM_API_MODE=http RUNTIME_API_BASE_URL=http://127.0.0.1:4100 npm run e2e -- tests/e2e/v0.1.11-application-integration-http.spec.ts` | 通过，3 tests |
| `E2E_RESET_DATABASE=true TEST_DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam_manual VITE_IAM_API_MODE=http RUNTIME_API_BASE_URL=http://127.0.0.1:4100 npm run e2e -- tests/e2e/v0.1.11-application-integration-http.spec.ts tests/e2e/runtime-api.spec.ts tests/e2e/v0.1.2-access-loop.spec.ts tests/e2e/v0.1.3-admin-console-http.spec.ts tests/e2e/v0.1.4-directory-http.spec.ts tests/e2e/v0.1.5-roles-http.spec.ts` | 通过，18 tests |
| `npm run e2e -- tests/e2e/access-loop.spec.ts` | 通过，3 tests |

## README 检查

README 已重构为简洁入口文档，覆盖：

- 项目价值
- 当前能力
- 技术栈
- 5 分钟本地启动
- 快速 Docker Compose 部署
- 第三方应用接入路径
- 安全边界
- 常用测试命令

README 未包含 `bpmt-120`、私有服务器路径、私有 IP、私有 nginx 配置或真实 secret。

## QA Loop

| 项目 | 结果 |
|---|---|
| QA 轮次 | 2 |
| 发现的问题 | E2E 初版选择器与当前 Ant Design 渲染不完全匹配。 |
| 已修复的问题 | 调整 v0.1.11 E2E 的应用选择、详情页进入路径和接入配置断言。 |
| 产品阻塞问题 | 0 |
| 剩余风险 | 全量 `npm run e2e` 不能在单一 HTTP mode 环境下直接覆盖 mock-only 和 HTTP-only 用例，需要继续分模式运行。 |

