# v0.1.13 OAuth 小收口部署报告

日期：2026-05-26

## 发布对象

- Commit: `544ae8e`
- Tag: `v0.1.13`
- Release: `https://github.com/wodenwang/feishu-iam/releases/tag/v0.1.13`
- Runtime image: `feishu-iam:v0.1.13`
- Pull Request: `https://github.com/wodenwang/feishu-iam/pull/14`
- GitHub Actions: `CI / PR #14 / 26447312409` 通过

## 部署方式

远端继续使用既有 Docker Compose runtime。部署时保留服务器侧 `.env*` 和 `data/postgres`，同步 `main` 当前源码层，并将 `FEISHU_IAM_IMAGE_TAG` 更新为 `v0.1.13`。

远端部署目录：

```text
bpmt-120:/home/bpmt/feishu-iam
```

## 线上验证

| 项目 | 结果 |
|---|---|
| `feishu-iam` container | `feishu-iam:v0.1.13`, healthy |
| `postgres` container | healthy |
| `curl http://127.0.0.1:8002/api/health` | `{"ok":true}` |
| `curl https://feishu-iam.riversoft.com.cn/api/health` | `{"ok":true}` |
| `GET http://127.0.0.1:8002/login` | HTTP 200 |
| `GET https://feishu-iam.riversoft.com.cn/login` | HTTP 200 |
| `schema_migrations` | `001_runtime`, `002_access_loop`, `003_thirdparty_oauth`, `004_oauth_pending_requests` |
| 远端 `.env` image tag | `FEISHU_IAM_IMAGE_TAG=v0.1.13` |
| 远端 `VERSION` | `0.1.13` |

## 备注

- Docker build 在远端完成，构建日志中仅出现既有 Vite chunk-size warning。
- 远端 `.env` 已在更新 image tag 前备份为 `.env.backup-v0.1.13-*`。
- 部署报告不记录任何真实飞书 App Secret、token、导出用户列表或同步快照。
