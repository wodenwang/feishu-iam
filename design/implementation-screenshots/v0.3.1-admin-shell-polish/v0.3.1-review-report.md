# v0.3.1 Pre-landing Review

## 结论

通过，未发现阻塞发布的问题。

## Diff 风险检查

- 身份边界：未新增 username/password、本地 root 或独立账号体系。
- 飞书边界：未改登录、session、飞书 OAuth、通讯录同步或事件回调逻辑。
- 权限边界：未改 route permission、button permission、应用管理员 scope 或后端权限校验。
- 数据边界：未新增 migration，未改数据库读写。
- UI 边界：只改 `AdminLayout`、`UserMenu` 和相关测试；运行信息迁移到用户下拉层，默认首屏不再显眼展示。
- E2E 边界：历史 HTTP E2E 登录页仍检查 runtime 信息；进入 Admin Shell 后检查用户菜单入口。

## 测试覆盖

- `src/router/routes.test.tsx`：Admin Shell 顶部栏低干扰、侧边栏收起/展开。
- `src/components/UserMenu/index.test.tsx`：下拉层展示角色、环境、runtime、open_id，trigger 不展示调试标签。
- `tests/e2e/v0.3.1-admin-shell-polish.spec.ts`：三视口验证首屏、折叠/展开和用户下拉。

## 剩余风险

- Ant Design 仍输出既有 chunk size warning，不是本次变更引入。
- 历史 E2E 之间存在测试数据污染风险，已在 QA 记录中注明并用隔离重跑验证受影响脚本。
