# v0.1.5 Roles HTTP QA Report

日期：2026-05-24
范围：`/roles` HTTP runtime-backed 角色管理。

## 结论

状态：已清零。

## QA Loop Metrics

- iterations run：1
- findings discovered：2
- findings fixed：2
- findings accepted/deferred by human：0
- findings remaining：0
- final status：cleared

## 覆盖路径

- 本地 mock 飞书登录。
- 平台管理员初始化。
- HTTP runtime 创建应用。
- 进入 `/roles`。
- 新建角色。
- 保存授权。
- 批量停用角色。
- 1440、1280、768 视口截图。
- 全量 frontend unit/component tests。
- 全量 server tests。
- production build。

## 发现与处理

1. 全量 `npm run test` 时 `RolesPage` 授权选择测试在并发回归中超过默认 5 秒。
   - 处理：该测试覆盖多步 Drawer/TreeSelect/Modal 交互，已单独放宽到 10 秒。
   - 复验：全量 `npm run test` 通过，18 files / 89 tests。
2. Vite dev server 报告 `Roles/index.tsx` 混合导出非组件 helper，影响 Fast Refresh。
   - 处理：将 role error formatter 移到 `src/pages/Roles/errors.ts`。
   - 复验：`npm run test -- src/pages/Roles/index.test.tsx` 和 `npm run build` 通过。

## 验证命令

```bash
TEST_DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam npm run server:test
npm run test
npm run build
E2E_RESET_DATABASE=true DATABASE_URL=postgres://feishu_iam:feishu_iam@127.0.0.1:55432/feishu_iam VITE_IAM_API_MODE=http npm run e2e -- tests/e2e/v0.1.5-roles-http.spec.ts
```

## 结果

- Server tests：12 files / 46 tests passed。
- Frontend tests：18 files / 89 tests passed。
- Build：passed；Vite 保留 chunk size warning。
- E2E：3 projects passed。
