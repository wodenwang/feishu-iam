# v0.2.0 应用接入生产化 QA 报告

日期：2026-05-27

## 结论

通过。后端、前端、脚本和浏览器主路径均已验证。QA 中发现的两个问题已修复并复验：768px 表格断字、最后管理员保护错误文案。

## 自动化验证

| 命令 | 结果 |
|---|---|
| `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test` | 通过，19 files / 95 tests |
| `npm test` | 通过，25 files / 126 tests |
| `npm run build` | 通过 |
| `npm run server:build` | 通过 |
| `git diff --check` | 通过 |
| `RUNTIME_API_BASE_URL=http://127.0.0.1:4100 bash scripts/verify-v0.2-application-onboarding.sh` | 通过 |

## 浏览器验证

运行环境：

- Backend：`FEISHU_AUTH_MODE=mock`
- Frontend：`VITE_IAM_API_MODE=http`
- Viewports：1440、1280、768

覆盖路径：

- mock 飞书登录和首次平台管理员初始化。
- 应用详情概览第一屏。
- 接入配置 Tab。
- OAuth redirect URI Table。
- 新增 redirect URI Drawer。
- redirect URI 停用确认。
- secret 轮换确认 Modal。
- secret 一次性结果 Modal。
- 应用管理员 Table。
- 移除最后管理员保护。
- 审计记录筛选。
- 768px 响应式布局。

## 不回归边界

- 未新增 `/directory` 编辑。
- 未新增飞书 webhook/event/incremental sync。
- 未新增完整 OIDC/JWKS/PKCE/refresh token。
- 未新增 SDK/CLI/Helm/Terraform。
- 未新增多租户 SaaS IdP 或 username/password。
