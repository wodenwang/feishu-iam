# Feishu IAM v1.0.0 Riversoft 正式版 checkpoint

## 会话目标

使用 `gstack /checkpoint` 保存 Feishu IAM v1.0.0 Riversoft 正式版发布、部署和 canary 收口状态，便于后续恢复上下文。

## 用户关键要求摘要

- 当前版本已通过 amd64 离线 tar 部署到 `bpmt@120.24.236.92:/home/bpmt/feishu-iam`。
- 公网入口为 `https://feishu-iam.riversoft.com.cn/`。
- GitHub Release 为 `https://github.com/wodenwang/feishu-iam/releases/tag/v1.0.0`。
- post-deploy canary 已通过 `/ready`、`/version`、未登录统一问题提示页、飞书登录入口、王文哲平台管理员权限、应用详情不暴露敏感凭证、request id 追踪入口和容器健康状态。
- 不记录真实 secret、token、cookie、飞书 app_secret、数据库密码或生产导出数据。

## 关键决策

- v1.0.0 是内部正式版发布收口，只保留前端 UI 翻新、Riversoft Logo 修正、QA 修复、管理员初始化和验收/会话归档相关产物。
- 数据初始化只处理 IAM 管理权限，确保“王文哲”获得 `platform_admin`，不初始化第三方应用权限、demo client、回调地址或业务角色。
- 线上正式飞书登录入口是 `/admin/auth/login`，`/admin/auth/feishu/callback` 是回调路径。
- registry 多架构推送问题不阻塞 v1.0.0 正式版，因为本次已采用 amd64 离线 tar 完成部署和验证。

## 发布证据

- GitHub Release：`https://github.com/wodenwang/feishu-iam/releases/tag/v1.0.0`
- 线上入口：`https://feishu-iam.riversoft.com.cn/`
- 远端部署目录：`bpmt@120.24.236.92:/home/bpmt/feishu-iam`
- canary 会话归档提交：`2fd091d docs: archive v1.0.0 canary observation`
- canary 归档文件：`docs/codex-sessions/2026-06-06-2320-v1.0.0-riversoft-canary.md`
- gstack 本地 checkpoint：`~/.gstack/projects/wodenwang-feishu-iam/checkpoints/20260606-2325-v100-riversoft-release-canary-complete.md`

## 线上验证结果

- `/ready` 返回 ready，数据库检查为 ok。
- `/version` 返回 `1.0.0`。
- 未登录后台入口展示统一问题提示页，包含 request id 和复制入口。
- `/admin/auth/login` 返回 302 并跳转飞书账号域名。
- 1440、768、390 三个视口未发现旧品牌文案、敏感凭证文本、横向溢出或非预期浏览器错误。
- 应用详情深链在未登录态没有暴露应用详情数据或敏感凭证。
- 远端容器状态正常，数据库容器 healthy。
- “王文哲”为 active 管理员，并拥有 `platform_admin`。
- `applications`、`application_clients`、`application_redirect_uris`、`iam_roles` 均未被 v1.0.0 初始化扩展。

## 剩余风险

- registry 多架构推送仍需后续单独处理；当前线上 v1.0.0 不依赖该路径。
- 当前 canary 覆盖未登录态和数据库边界；如果后续需要覆盖真实已登录后台路径，需要使用安全的人工登录或受控会话，不应记录 cookie。

## 后续低频观察建议

- 发布后第一个工作日再执行一次 `/ready`、`/version`、后台未登录入口、飞书登录入口和容器健康检查。
- 首位管理员完成真实飞书登录后，人工确认应用详情、追踪页和系统管理入口可用，同时避免输出任何会话凭证。
- 后续任何新 slice 均重新进入 harness 流程，不把 v1.0.0 收口范围继续扩大。

## 本次修改文件

- 新增本会话归档：`docs/codex-sessions/2026-06-06-2334-v1.0.0-riversoft-checkpoint.md`
- 新增本地 gstack checkpoint：`~/.gstack/projects/wodenwang-feishu-iam/checkpoints/20260606-2325-v100-riversoft-release-canary-complete.md`

## 验证

- 已确认执行前工作区为 `main...origin/main` 且干净。
- 本会话未修改业务代码、部署配置、迁移、release、tag 或线上服务。
