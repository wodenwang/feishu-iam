# v0.1.3 HTTP mode `/design-review` 记录

验证日期：2026-05-24

范围：仅审查 v0.1.3 第一个 vertical slice 的已实现 HTTP mode UI：`/login`、`/initialize`、`/applications`、one-time secret modal、`/audit-logs`、requestId 详情，以及已在 `/browse` 覆盖的 loading、empty、API error、401、403、409/requestId 状态。未进入 Roles/Directory、Dashboard/Sync、Application Detail/Onboarding 后续切片。

## 设计基线

- 产品类型：企业级 Admin Console。
- 组件体系：Ant Design。
- 目标：高信息密度、表格优先、操作路径清晰、状态完整、错误可恢复。
- 参考文档：`DESIGN.md`、`AGENTS.md`、`design/implementation-screenshots/browse/v0.1.3-browse-verification.md`。

## 发现与处理

| 编号 | 等级 | 页面 | 问题 | 处理 |
|---|---|---|---|---|
| FINDING-001 | High | `/applications` 768 视口 | 顶部 Header 在窄屏下把品牌和 Feishu User ID 挤压成多行，影响后台首屏可信度和扫读效率。 | 已修复。窄屏 Header 只保留 `HTTP runtime`、显示名和角色；桌面继续显示完整 Feishu User ID。 |

## 验证证据

修复后截图：

- `finding-001-after-applications-768-final.png`
- `applications-1280-after.png`
- `applications-1440-after.png`

命令证据：

- `npm run test -- src/router/routes.test.tsx src/pages/Login/index.test.tsx src/pages/Initialize/index.test.tsx`
  - 结果：3 个 test files 通过，10 个 tests 通过。
- `npm run build`
  - 结果：构建通过；Vite 仅提示 chunk size warning，非本次修复回归。
- gstack `/browse`
  - `768x1024` 访问 `/applications`，console errors：无，network 4xx/5xx：无。

## 结论

本轮 `/design-review` 阻塞问题已清除。v0.1.3 HTTP mode 第一个 vertical slice 可以进入 gstack `/qa`。
