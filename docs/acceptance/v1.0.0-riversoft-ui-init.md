# v1.0.0 Riversoft UI 翻新与管理员初始化验收

## 范围

- 前端 UI 翻新：通过 Riversoft token 和项目级组件完成正式版质感升级。
- 数据初始化：确保 `INITIAL_PLATFORM_ADMIN_FEISHU_USER_ID` 指向的“王文哲”拥有 `platform_admin`。

## 不做

- 不改变 UX 流程、信息架构、路由、表单流程、数据契约、CRUD 行为、权限逻辑或业务逻辑。
- 不初始化第三方应用权限、demo client、回调地址或业务角色。
- 不记录真实 secret、token、cookie、飞书 app_secret、数据库密码或生产导出数据。

## UI 验收

- [x] 1440 桌面：左侧 Sidebar + 内容工作区稳定，无深色 topbar 误导。
- [x] 768 平板：导航可收缩，应用详情 Tab 和凭证状态表达不溢出。
- [x] 390 窄屏：移动 Header + Sheet 导航，表格横向滚动，触控目标不小于 44px。
- [x] 状态矩阵：空、错、加载、禁用、提交中、危险确认均有稳定展示。
- [x] 可访问性：focus ring、`aria-current`、`aria-expanded`、图标按钮 `aria-label`、44px 触控目标通过检查。

## 数据初始化验收

- [ ] `INITIAL_PLATFORM_ADMIN_FEISHU_USER_ID` 使用“王文哲”的飞书 `user_id`，不写入仓库。
- [ ] `/api/v1/admin/me` 返回角色包含 `platform_admin`。
- [x] 迁移不创建第三方应用、client、回调地址、权限点、权限组或业务角色。

## 验证命令

```bash
pnpm --filter @feishu-iam/admin-web typecheck
pnpm --filter @feishu-iam/admin-web lint
pnpm --filter @feishu-iam/admin-web test
pnpm --filter @feishu-iam/admin-web test:buttons
pnpm --filter @feishu-iam/admin-web test:responsive
pnpm check
```

## 本轮验证记录

验证时间：2026-06-06。

命令验证：

- `pnpm --filter @feishu-iam/admin-web typecheck`：通过。
- `pnpm --filter @feishu-iam/admin-web lint`：通过。
- `pnpm --filter @feishu-iam/admin-web test`：通过，15 个测试文件、156 个测试。
- `pnpm --filter @feishu-iam/admin-web test:buttons`：通过。
- `ADMIN_WEB_URL=http://localhost:5173 pnpm --filter @feishu-iam/admin-web test:responsive`：通过，覆盖 390、768、1280、1440。
- `pnpm --filter @feishu-iam/api test -- test/version.controller.e2e-spec.ts`：通过。
- `pnpm check`：通过，API 41 个测试文件、460 个测试；admin-web 15 个测试文件、156 个测试。

Playwright 浏览器证据：

- `docs/acceptance/v1.0.0-riversoft-browser/workbench-1440.png`
- `docs/acceptance/v1.0.0-riversoft-browser/app-detail-768.png`
- `docs/acceptance/v1.0.0-riversoft-browser/trace-390.png`
- `docs/acceptance/v1.0.0-riversoft-browser/playwright-check.json`

浏览器检查结果：

- 1440 工作台：左侧主菜单可见，当前导航可见，无页面级横向溢出。
- 768 应用详情：窄屏打开导航按钮可见，无页面级横向溢出，未展示疑似敏感凭证形态。
- 390 追踪页：窄屏打开导航按钮可见，request id 主旅程可见，无页面级横向溢出。
- 三个视口均无 console error、无 request failed、无疑似敏感凭证形态。

数据初始化检查：

- `migrations/V1_0_0__platform_admin_initialization.sql` 只写入 `admin_users`、`admin_user_roles`、`audit_logs`、`schema_versions`。
- 迁移复用 `INITIAL_PLATFORM_ADMIN_FEISHU_USER_ID`，不新增 secret env。
- 静态边界扫描只命中 `admin_roles`、`admin_user_roles`，未命中第三方应用、client、回调地址、权限点、权限组或敏感凭证初始化。
