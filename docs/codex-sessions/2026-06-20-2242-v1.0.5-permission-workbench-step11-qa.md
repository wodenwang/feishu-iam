# v1.0.5 权限管理角色配置工作台 step11 QA

## 会话目标

按 my-harness 执行 Feishu IAM `v1.0.5 - 权限管理角色配置工作台` 的 step11 `gstack /qa`，完成系统化功能 QA 后停止，不进入 step12。

## 用户原始关键要求摘要

- my-harness step1-step10 已完成。
- 本次只执行 step11 `gstack /qa`。
- 完成后停止等待，不进入 step12 `gstack /review`。
- 不重复 step10，不进入 ship、commit、push、release 或 deploy。
- QA 覆盖角色列表、角色配置工作台、组织与用户绑定、应用权限绑定、应用详情移除角色管理、旧 deep link、管理员权限边界、安全审计和回归风险。

## 本次关键约束

- 遵守 `AGENTS.md` 和 `DESIGN.md`。
- 不进入 Plan mode。
- 不调用 AskUserQuestion、`request_user_input` 或交互式选择工具。
- `/qa` 原始流程默认会修复并提交，但本轮按用户边界只做 QA 和证据记录，不修改业务代码。
- 未获得授权，不执行 commit、push、release 或 deploy。

## QA 结论

step11 已完成。

状态：`DONE_WITH_CONCERNS`

未发现 P0/P1 功能缺陷。残余风险是浏览器 QA 使用 mock 后端，不等同于真实生产数据、真实管理员会话和 112 服务器停机升级验收。

## 修改过的文件

本轮没有修改业务代码。

新增或更新 QA / harness / 归档文件：

- `.gstack/qa-reports/v1.0.5-step11/report.md`
- `.gstack/qa-reports/v1.0.5-step11/browser-report.json`
- `.gstack/qa-reports/v1.0.5-step11/browser-qa.cjs`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/`
- `.my-harness/status.md`
- `.my-harness/runs/2026-06-20-v1.0.5-permission-workbench.md`
- `docs/codex-sessions/2026-06-20-2242-v1.0.5-permission-workbench-step11-qa.md`

## 执行过的关键命令和结果

### 自动化验证

```bash
pnpm check
```

结果：

- typecheck 通过。
- lint 通过。
- API：41 个测试文件，483 个测试通过。
- Admin Web：17 个测试文件，156 个测试通过。

### 迁移兼容

使用临时 `postgres:16-alpine` 容器构造 pre-v1.0.5 最小 schema，并连续执行两次：

```bash
migrations/V1_0_5__role_application_bindings.sql
```

结果：

```text
role_applications=1
iam_roles_application_id_column=0
schema_version_105=1
group_fk=1
point_fk=1
```

### 浏览器 QA

```bash
NODE_PATH=/Users/wenzhewang/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules node .gstack/qa-reports/v1.0.5-step11/browser-qa.cjs
```

结果：

```json
{
  "failures": [],
  "consoleErrors": [],
  "pageErrors": [],
  "requestFailures": [],
  "responseErrors": []
}
```

## 浏览器证据

- `.gstack/qa-reports/v1.0.5-step11/screenshots/01-role-list-platform.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/02-role-list-actions.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/03-workbench-subjects.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/04-workbench-app-permissions.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/05-old-deep-links.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/06-application-permission-assets.png`
- `.gstack/qa-reports/v1.0.5-step11/screenshots/07-application-admin-boundary.png`

## 未完成事项和下一步建议

下一步是 my-harness step12 `gstack /review`。

step12 应重点审查：

- v1.0.5 的完整 diff 风险。
- 数据模型和迁移风险。
- API 边界、权限校验和审计日志。
- 前端路由、URL 状态和兼容跳转。
- 应用管理移除角色管理后的回归风险。
- 测试覆盖缺口和发布风险。

本轮已按用户要求停止在 step11。
