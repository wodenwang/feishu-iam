# v0.1.17 接入闭环验收包 QA 记录

范围：`v0.1.17` 第一个 vertical slice，覆盖当前版本自动验收脚本、README / Demo 文档接线和本地验证证据。

## 结论

通过。`scripts/verify-v0.1-access-loop.sh` 已在 `0.1.17` 版本元数据下，于干净 mock Feishu runtime 上跑通当前 v0.1 接入闭环：

- health check
- 平台管理员 mock 登录和首次绑定
- sync status / preflight
- 应用创建
- Application API 权限组/权限点注册
- 角色授权
- OAuth authorize/token
- bearer token + HMAC 权限查询
- allow/deny 权限结果
- 关键审计动作存在

本切片没有修改前端 UI 代码，因此未新增浏览器截图。浏览器人工验收路径已写入 README 和 Demo 文档，后续 release 收口如需要可补截图。

## 验证命令

```bash
bash -n scripts/verify-v0.1-access-loop.sh
node --check examples/thirdparty-demo/src/server.js
git diff --check
```

结果：通过。

```bash
docker run --rm -d --name feishu-iam-v017-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=feishu_iam_test \
  -p 55432:5432 \
  postgres:16-alpine

docker exec feishu-iam-v017-postgres psql -U postgres -d postgres \
  -c 'drop database if exists feishu_iam_v017' \
  -c 'create database feishu_iam_v017'
```

结果：PostgreSQL ready，`feishu_iam_v017` 创建成功。

```bash
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_v017 \
SESSION_SECRET=local-session-secret-at-least-32-bytes \
FEISHU_AUTH_MODE=mock \
npm run server:dev
```

结果：runtime 启动成功，`GET /api/health` ready。

```bash
RUNTIME_API_BASE_URL=http://127.0.0.1:4100 \
bash scripts/verify-v0.1-access-loop.sh
```

结果：通过。

安全输出摘要：

```text
v0.1 access loop verified against http://127.0.0.1:4100
appKey=app_8c6823b55a474d03
allowedPermissions=["demo.customer:view"]
deniedPermissions=[]
syncHealth=unknown
OK: v0.1 access loop verified at http://127.0.0.1:4100
```

```bash
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test
```

结果：19 files / 89 tests passed。

```bash
npm test
```

结果：25 files / 118 tests passed。过程中 jsdom 输出 `getComputedStyle` pseudo-element warning，未导致失败。

```bash
npm run build
```

结果：通过。保留既有 Vite chunk size warning。

```bash
npm run server:build
```

结果：通过。

## 纠正记录

- 初次误用了 Jest 参数 `npm test -- --runInBand`，Vitest 不支持，已改为 `npm test`。
- 初次 server tests 在 PostgreSQL 未启动前运行，出现 `ECONNREFUSED 127.0.0.1:55432`，启动隔离 PostgreSQL 后重跑通过。
- 初次 `git diff --check` 发现 `IMPLEMENTATION_PLAN.md` EOF 多空行，已修复并重跑通过。

## QA 循环

- 循环次数：1
- 发现问题：3 个验证/环境问题
- 已修复：3 个
- 剩余问题：0
- 状态：cleared
