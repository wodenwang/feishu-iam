# Codex 会话归档：v1.0.5 权限管理角色配置工作台 step15 部署

## 会话目标

按 my-harness 流程继续 Feishu IAM `v1.0.5 - 权限管理角色配置工作台`，完成 step15 `gstack /land-and-deploy`，直到 GitHub Release 对应版本完成远端部署和生产验证。

## 用户关键要求摘要

- 严格按 my-harness 推进到发布、release 和远端部署完成。
- 使用 gstack 流程，但不进入 Plan mode。
- 不调用 AskUserQuestion、request_user_input 或任何交互式选择工具。
- 不记录或输出 secret、token、cookie、authorization、授权码或敏感 raw payload。
- 完成远端部署后更新 `.my-harness/` 快速索引和相关发布文档。

## 关键约束

- 项目采用单机 Docker Compose 停机升级，不走 PR merge 后自动部署。
- release commit 为 `ed98409`，tag 为 `v1.0.5`。
- 生产部署目录为 `bpmt@120.24.236.92:/home/bpmt/feishu-iam`。
- 本次部署使用离线镜像包，不依赖生产服务器拉取远端镜像。

## 重要决策和原因

- 远端升级使用 `FEISHU_IAM_GIT_SYNC=false`：避免生产目录在部署中自动切到 post-release 文档提交，确保运行物与 `v1.0.5` release commit `ed98409` 对齐。
- 远端升级使用 `FEISHU_IAM_PULL_POLICY=never`：生产服务器使用已传输并 load 的镜像，避免临时 registry 或网络波动影响部署。
- step15 只执行生产健康、迁移和路由 smoke：本地 step9-step11 已覆盖浏览器与功能 QA，生产阶段不额外扩大为真实管理员全量交互测试。

## 修改过的文件

- `CHANGELOG.md`
- `README.md`
- `.my-harness/status.md`
- `.my-harness/runs/2026-06-20-v1.0.5-permission-workbench.md`
- `.my-harness/step15-land-and-deploy.md`
- `docs/codex-sessions/2026-06-20-2359-v1.0.5-permission-workbench-step15-deploy.md`

## 关键命令和验证结果

```bash
scp /tmp/feishu-iam-v1.0.5-linux-amd64.tar bpmt@120.24.236.92:/home/bpmt/feishu-iam/
```

结果：传输完成。

```bash
ssh bpmt@120.24.236.92 'cd /home/bpmt/feishu-iam && docker load -i feishu-iam-v1.0.5-linux-amd64.tar && FEISHU_IAM_GIT_SYNC=false FEISHU_IAM_PULL_POLICY=never FEISHU_IAM_IMAGE_TAG=v1.0.5 APP_VERSION=1.0.5 GIT_COMMIT=ed98409 ./upgrade.sh'
```

结果：`Loaded image: feishu-iam:v1.0.5`，升级备份目录为 `/home/bpmt/feishu-iam/backups/20260620-235502`，`V1_0_5__role_application_bindings.sql` 已应用，web 容器启动完成。

```bash
curl -fsS https://feishu-iam.riversoft.com.cn/ready
curl -fsS https://feishu-iam.riversoft.com.cn/version
```

结果：

```json
{"status":"ready","checks":{"database":"ok"}}
{"name":"feishu-iam-api","version":"1.0.5","commit":"ed98409","node_env":"production"}
```

数据库只读验证：

- `schema_versions` 包含 `1.0.5`。
- `iam_role_applications` 存在并包含 3 条绑定。
- 未绑定角色数为 0。
- 旧 `iam_roles.application_id` 已移除。
- 权限组 / 权限点绑定没有脱离 `iam_role_applications` 的记录。

路由 smoke：

- `/admin/permissions` 返回 `200 OK`。
- 旧角色 deep link `tab=subjects` 返回 `200 OK`。
- 旧应用详情 `tab=roles` 返回 `200 OK`。

## 未完成事项

- 生产目录同时存在 `docker-compose.yml` 和 `docker-compose.yaml`，后续可单独清理以降低运维歧义。
- 本次未做真实管理员线上登录后的完整交互回归；如需要，可另开 canary 任务。

## 下一步建议

`v1.0.5` 的 my-harness step1-step15 已完成。后续如继续推进，应另起新版本或针对生产观察到的问题进入只读诊断。
