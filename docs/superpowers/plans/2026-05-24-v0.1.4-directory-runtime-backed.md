# feishu-iam v0.1.4 Directory runtime-backed 只读浏览 Implementation Plan

当前执行计划见仓库根目录 `IMPLEMENTATION_PLAN.md`。

本文件用于满足 `docs/superpowers/plans/` 的计划归档约定。`IMPLEMENTATION_PLAN.md` 是本切片的可执行计划，覆盖：

- `/directory` HTTP mode 部门树。
- 用户分页列表。
- 部门筛选。
- 用户详情 Drawer。
- loading / empty / API error / 401 / 403 / requestId。
- 后端 directory contract、前端 DTO mapping、HTTP service、页面状态和 E2E 验证。

非目标：

- Roles 创建、编辑、授权保存。
- Sync。
- Dashboard。
- Application Detail。
- Onboarding。
- 真实飞书 OAuth。
- push、PR、merge、release、tag、deploy。
