# v0.1.10 Admin Console 前端重构 QA 记录

日期：2026-05-24

## 结论

通过。HTTP 后端链路、mock 可视链路、前端单测、服务端测试和生产构建均已完成。

## QA 循环指标

- QA 循环：2 次。
- 发现问题：4 个。
- 已修复：4 个。
- 遗留：0 个。

## 问题与处理

- HTTP E2E 初始失败：浏览器端错误设置跨域 `VITE_IAM_API_BASE_URL` 导致 CORS，改回 Vite `/api` proxy 验证方式。
- E2E 文案漂移：本地开发登录按钮按原型变更为 `Mock 开发登录（仅本地）`，已更新测试。
- v0.1.2 API smoke 读取旧应用响应 envelope，已改为从 `application` envelope 中读取 `app_key`。
- 768 视口隐藏 `HTTP runtime` 标签，测试改为接受 Header 中的 `生产环境` 响应式状态。

## 验证命令

- `npm test`：25 files / 112 tests passed。
- `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55433/feishu_iam_test npm run server:test`：16 files / 62 tests passed。
- `npm run build`：passed，保留 Vite chunk size warning。
- `npm run server:build`：passed。
- `E2E_RESET_DATABASE=true TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55433/feishu_iam_test VITE_IAM_API_MODE=http RUNTIME_API_BASE_URL=http://127.0.0.1:4100 npx playwright test tests/e2e/runtime-api.spec.ts tests/e2e/v0.1.2-access-loop.spec.ts tests/e2e/v0.1.3-admin-console-http.spec.ts tests/e2e/v0.1.4-directory-http.spec.ts tests/e2e/v0.1.5-roles-http.spec.ts`：15 passed。
- `npx playwright test tests/e2e/access-loop.spec.ts`：3 passed。

## 环境说明

- 临时测试库：`feishu-iam-v010-test-postgres`，端口 `55433`。
- 本地后端：`127.0.0.1:4100`。
- 本地前端截图服务：`127.0.0.1:5174`。
