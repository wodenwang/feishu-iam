# v0.1.11 应用接入配置部署报告

时间：2026-05-25 Asia/Shanghai

## 版本

- GitHub PR: `#11`
- Merge commit: `554aa62`
- Tag: `v0.1.11`
- Release: `https://github.com/wodenwang/feishu-iam/releases/tag/v0.1.11`
- Runtime image: `feishu-iam:v0.1.11`

## 部署方式

远端继续使用既有 Docker Compose runtime。部署时保留服务器侧 `.env*` 和 `data/postgres`，只替换源码层并将 `FEISHU_IAM_IMAGE_TAG` 更新为 `v0.1.11`。

由于远端没有 `rsync`，本次使用 tar 流上传源码。解包时出现 macOS extended header 警告，不影响源码内容和镜像构建。

## 远端验证

| 检查项 | 结果 |
|---|---|
| `docker compose up -d --build` | 通过 |
| `feishu-iam` container | `feishu-iam:v0.1.11`, healthy |
| `postgres` container | healthy |
| `curl http://127.0.0.1:8002/api/health` | `{"ok":true}` |
| 远端 `VERSION` | `0.1.11` |
| 远端 `.env` image tag | `FEISHU_IAM_IMAGE_TAG=v0.1.11` |

## 备注

- 第一次 health curl 在应用容器刚启动时返回 connection reset；等待容器 health check 稳定后复查通过。
- 部署报告不记录任何真实飞书 App Secret、token、导出用户列表或同步快照。

