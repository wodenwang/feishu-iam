# v0.4.0 Agent 接入初始化提示词 QA 记录

日期：2026-05-28

## 验证环境

- Branch: `codex/v0.4.0-agent-onboarding-prompt`
- Backend: `http://127.0.0.1:4100`
- Frontend: `http://127.0.0.1:5174`
- Database: `postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test`
- Auth mode: `FEISHU_AUTH_MODE=mock`

## 自动化验证

```bash
npm test -- src/pages/Applications/Detail.test.tsx src/pages/Applications/List.test.tsx src/pages/Applications/Onboarding.test.tsx src/features/iam/httpApi.test.ts
```

结果：通过，4 files / 46 tests passed。覆盖普通占位符提示词、创建后一键复制一次性明文提示词、轮换 appSecret/API secret 一次性提示词和 HTTP copy kind。

```bash
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/applications.test.ts
```

结果：通过，1 file / 24 tests passed。覆盖 `runtime_env`、`agent_prompt_placeholder`、`agent_prompt_onetime_plaintext` 审计类型，确认不接收或记录前端提交的 secret 明文。

```bash
npm run build
npm run server:build
git diff --check
```

结果：全部通过。`npm run build` 保留既有 Vite chunk size warning。

```bash
npm test
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test
```

结果：通过，前端 25 files / 133 tests passed，后端 19 files / 107 tests passed。期间 jsdom 输出既有 `getComputedStyle() with pseudo-elements` 提示，不影响测试结果。

## 浏览器验证

Playwright 检查：

- 创建第三方应用后的一次性 secret modal 显示 `复制一次性 Agent 初始化提示词`，提示明文 secret 只允许写入运行时环境或 secret manager，不得提交到 Git。
- 应用详情页 `接入配置` 中普通提示词卡片显示 `复制应用提示词`，说明 Codex/Claude Code 可生成 `AGENTS.md` / `CLAUDE.md`，普通提示词只使用 secret 占位符。
- 轮换 appSecret 后的一次性 modal 显示本次 appSecret 明文和 `复制一次性 Agent 初始化提示词`。
- 轮换 API secret 后的一次性 modal 显示本次 API secret 明文和 `复制一次性 Agent 初始化提示词`。
- 重新加载应用详情页后 Browser console 无 error / warning。

截图：

- `placeholder-prompt-card.png`
- `app-secret-onetime-prompt.png`
- `api-secret-onetime-prompt.png`

## 结论

`v0.4.0` Agent 接入初始化提示词通过本地自动化和浏览器验证。该版本不改变 OAuth 授权语义、不新增本地账号体系、不持久化 secret 明文，只在创建或轮换结果的一次性前端上下文中把对应 secret 写入初始化提示词。
