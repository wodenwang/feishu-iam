# v1.0.5 权限管理角色配置工作台 step14 ship

## 会话目标

按 my-harness step14 执行 `gstack /ship`，完成 Feishu IAM `v1.0.5` 的最终验证、提交、推送、tag 和 GitHub Release。

## 用户关键要求

- 严格按 my-harness 流程推进。
- 不进入 Plan mode。
- 不调用 AskUserQuestion、request_user_input 或交互式选择工具。
- 本轮目标继续推进到 release 和远端部署。
- 不输出或记录 secret、token、cookie、authorization、授权码或敏感 raw payload。

## 关键动作

- 使用 coverage 子代理执行覆盖率审计，基线约 `72%`。
- 使用 plan completion 子代理执行计划完成审计，结果 `44/44` 完成，`0 deferred`，`0 unverifiable`。
- 根据覆盖率审计补充 focused tests：
  - `apps/admin-web/src/api/permission.test.ts`
  - `apps/admin-web/src/features/permissions/PermissionManagementView.test.tsx`
  - `apps/api/test/permission-calculation.service.spec.ts`
- 手动执行 pre-landing review，重点审查 SQL / 数据安全、权限边界、安全脱敏和前端关键路径。
- 更新 README、CHANGELOG 和 `.my-harness/` 快速索引。
- 创建 release commit、推送 feature branch、fast-forward `main`、推送 `main`、创建并推送 `v1.0.5` tag、创建 GitHub Release。

## 验证结果

```bash
pnpm --filter @feishu-iam/admin-web test -- src/api/permission.test.ts src/features/permissions/PermissionManagementView.test.tsx
pnpm --filter @feishu-iam/api test -- test/permission-calculation.service.spec.ts
pnpm check
pnpm build
git diff --cached --check
```

结果：

- focused tests 通过：Admin Web 2 个文件 10 个用例；API 1 个文件 11 个用例。
- `pnpm check` 通过：后端 41 个测试文件 484 个用例，前端 18 个测试文件 159 个用例。
- `pnpm build` 通过：后端 NestJS 构建完成，前端 Vite 构建完成；Vite chunk size warning 为既有提示。
- staged diff 空白检查通过。
- staged diff 敏感扫描未发现真实凭证；唯一剩余命中为 `subagent-driven-development` 文档词。

## Release 结果

- release commit：`ed98409 feat: add permission role workbench v1.0.5`
- feature branch：`codex/v1.0.5-permission-workbench`
- tag：`v1.0.5`
- GitHub Release：`https://github.com/wodenwang/feishu-iam/releases/tag/v1.0.5`

## 未完成事项

- step15 `gstack /land-and-deploy` 尚未执行。
- 生产镜像 digest、远端升级备份、迁移读回、`/ready` 和 `/version` 线上验证需要在 step15 记录。
