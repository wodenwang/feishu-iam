# v0.2.2 部署记录

## 发布对象

- 版本：`v0.2.2`
- 发布主题：应用接入诊断与可观测性补强
- 发布提交：`3cd2d0013434acc5f6f4f234fec982073b74923b`
- Pull Request：`https://github.com/wodenwang/feishu-iam/pull/22`
- PR GitHub Actions：`https://github.com/wodenwang/feishu-iam/actions/runs/26530540197`，结论 `success`
- main GitHub Actions：`https://github.com/wodenwang/feishu-iam/actions/runs/26530798448`，结论 `success`
- GitHub Release：`https://github.com/wodenwang/feishu-iam/releases/tag/v0.2.2`
- 部署时间：`2026-05-28 02:37 CST`

## 部署环境

- 目标主机：`bpmt-120`
- 目标目录：`/home/bpmt/feishu-iam`
- Compose 服务：`feishu-iam`、`postgres`
- 应用镜像：`feishu-iam:v0.2.2`
- 镜像 ID：`sha256:ef9ae8ab3b3a9142db8a79dab65e7d347825a470547433c3df44f80b40e661ed`
- 对外端口：`8002 -> 4100`
- 公网入口：`https://feishu-iam.riversoft.com.cn`

## 部署动作

1. 使用 `git archive --format=tar HEAD` 将仓库受控文件同步到远端部署目录。
2. 保留远端 `.env` 与 `data/postgres`，并创建 `.env.backup-v0.2.2-*`。
3. 将远端 `.env` 中的 `FEISHU_IAM_IMAGE_TAG` 更新为 `v0.2.2`。
4. 在远端执行 `docker compose up -d --build --remove-orphans` 完成镜像构建、容器重建和启动。

## 验证证据

- 远端 Compose 状态：

```text
feishu-iam            feishu-iam:v0.2.2    Up (healthy)    0.0.0.0:8002->4100/tcp
feishu-iam-postgres   postgres:16-alpine   Up (healthy)    5432/tcp
```

- 远端本机健康检查：

```json
{"ok":true}
```

- 公网健康检查：

```json
{"ok":true}
```

- 公网登录页：

```text
HTTP 200，返回 Vite HTML。
```

- 已应用数据库迁移：

```text
001_runtime
002_access_loop
003_thirdparty_oauth
004_oauth_pending_requests
005_application_admins
006_sync_runs
007_sync_ops
008_application_onboarding
009_role_permission_groups
```

## 边界确认

- 本版本只交付应用接入诊断、脱敏诊断包和复制审计。
- 系统仍保持飞书唯一身份源，未新增 username/password、本地 root 或独立账号体系。
- 诊断 API 不返回 secret、token、authorization code、signature、cookie 或 hash 原文。
- 远端 `.env`、飞书应用凭证、tokens、同步快照和 PostgreSQL 数据目录未纳入仓库同步。
