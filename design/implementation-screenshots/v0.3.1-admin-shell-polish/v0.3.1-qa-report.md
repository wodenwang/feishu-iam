# v0.3.1 QA 记录

## 结论

通过。`v0.3.1 Admin Shell 外框与顶部栏修复` 已完成单元、构建、后端回归、浏览器交互和受影响 E2E 验证。

## 验证命令

```text
npm test
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test
npm run build
npm run server:build
git diff --check
E2E_RESET_DATABASE=true TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_e2e DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_e2e SESSION_SECRET=local-session-secret-at-least-32-bytes FEISHU_AUTH_MODE=mock VITE_IAM_API_MODE=http RUNTIME_API_BASE_URL=http://127.0.0.1:4100 npx playwright test tests/e2e/v0.3.1-admin-shell-polish.spec.ts
E2E_RESET_DATABASE=true TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_e2e DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_e2e SESSION_SECRET=local-session-secret-at-least-32-bytes FEISHU_AUTH_MODE=mock VITE_IAM_API_MODE=http RUNTIME_API_BASE_URL=http://127.0.0.1:4100 npx playwright test tests/e2e/v0.1.4-directory-http.spec.ts --workers=1
```

## 结果

| 验证项 | 结果 |
|---|---|
| 前端单元测试 | 25 files / 132 tests passed |
| 后端测试 | 19 files / 104 tests passed |
| 前端构建 | passed |
| 后端构建 | passed |
| whitespace 检查 | passed |
| v0.3.1 三视口 E2E | 3 passed |
| Chrome DevTools 快照 | `/applications` 默认态、折叠态、用户下拉态通过，console 无 error/warn |

## 补充说明

- 组合运行历史 E2E 时，`v0.1.4-directory` 与 `v0.1.15-sync` 的测试数据存在顺序污染：同步测试写入目录用户后，目录空部门断言 `IT 部 (0)` 不再成立。
- 将 `v0.1.4-directory` 单独重置数据库运行后，desktop / laptop / tablet 三视口全部通过。该问题不是 `v0.3.1` 外框变更导致。
