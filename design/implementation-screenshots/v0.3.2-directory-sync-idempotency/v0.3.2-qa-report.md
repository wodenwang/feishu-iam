# v0.3.2 组织同步幂等修复 QA 记录

日期：2026-05-28

## 验证环境

- Branch: `codex/v0.3.2-directory-sync-idempotency`
- Backend: `http://127.0.0.1:4100`
- Frontend: `http://127.0.0.1:5174`
- Database: `postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test`
- Auth mode: `FEISHU_AUTH_MODE=mock`

## 自动化验证

```bash
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/sync.test.ts
```

结果：通过，`server/tests/sync.test.ts` 17 tests passed。覆盖 #27 历史 seed 部门隐藏、连续 full sync 幂等、合法同名部门保留。

```bash
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test
```

结果：通过，19 files / 107 tests passed。

```bash
npm test
```

结果：通过，25 files / 132 tests passed。期间 jsdom 输出既有 `getComputedStyle() with pseudo-elements` warning，不影响测试结果。

```bash
npm run build
npm run server:build
git diff --check
```

结果：全部通过。`npm run build` 保留既有 Vite chunk size warning。

## 浏览器验证

准备数据：

- 清空本地测试库并重新运行 migrations。
- 使用 mock 飞书用户 `ou_v032_browser_admin` 绑定平台管理员。
- 插入 stale 部门 `dept_stale_it_browser / it部 / active`。
- 触发 `POST /api/sync/runs`，LocalMock full sync 成功后 stale 部门被标记为 disabled。
- `GET /api/directory/departments?page=1&pageSize=100` 返回 active 部门 `dept_it` 和 `dept_sales`，不返回 stale 部门。
- `GET /api/sync/status` 返回 `directoryDepartmentCount: 2`，同步状态摘要不再统计 disabled stale 部门。

Playwright 检查：

- `/directory` 1440px：页面正常渲染，部门树显示 active 部门 `IT 部`、`销售部`，不包含 `dept_stale_it_browser`。
- `/sync` 1440px：同步状态摘要、飞书事件同步和同步任务区域正常渲染，部门数量显示 active 计数 `2`。
- Browser console：无 error / warning。

截图：

- `directory-1440.png`
- `sync-1440.png`

## 结论

`v0.3.2` 的组织同步幂等修复通过本地自动化和浏览器验证。该版本没有新增目录编辑、目录治理、事件 worker、OIDC、SDK/CLI 或本地账号体系。
