# Feishu IAM v0.14.0 后台详情页重构落地前审查

日期：2026-05-28
审查方式：本机手工代码审查。当前环境没有可用的 `gstack` 命令，因此用等价的 diff 阅读、测试、构建和响应式浏览器检查替代自动 review skill。

## 结论

未发现阻塞发布的问题。当前剩余工作是发布链路本身：提交、推送、MR 合并、镜像、tag、release 和 112 停机升级验证。

## 重点检查

- 路由边界：新增 `/admin/applications/:appKey` 和 `/admin/permissions/:appKey/roles/:roleId`，未改变后端 API、DDL、Prisma schema 或 SSO 协议面。
- 导航边界：`系统管理` 父级改为单一整行按钮，消除“父级链接 + chevron”重复控件；当前系统二级页仍能保持父级展开。
- 兼容边界：旧 `sheet=app:*` 和 `sheet=role:*` 仍保留，历史深链不会直接失效。
- 权限边界：权限管理未恢复角色元数据新增、编辑或启停，角色元数据仍归应用详情维护。
- 安全边界：文档和测试没有新增明文 secret、token、cookie 或真实密码；接入提示词仍是安全版文本。
- 发布边界：版本元数据已切到 `0.14.0` / `v0.14.0`，但 README/CHANGELOG 中 digest 和 112 证据需等镜像和部署完成后补齐。

## 风险

- 应用详情当前是独立页面中的配置分区，不是 URL Tab 化页面；规格已同步调整为本版本交付范围。
- `design/admin-console-v0.11.3.pen` 当前工作区存在较大未提交变更，发布提交应排除该文件，除非明确把它作为原型变更纳入本版本。
