# v0.1.16 同步运营 Runtime QA 报告

范围：backend status/preflight、scheduled full sync system actor、`/sync` frontend health/preflight UI。

## 结论

通过。当前切片的关键用户路径、权限边界和回归测试均通过。

## QA 循环

- 循环次数：1
- 发现问题：1
- 已修复：1
- 剩余问题：0
- 状态：cleared

发现并修复的问题：浏览器 console 报 Ant Design v6 `Alert.message` deprecated，已改为 `Alert.title` 并重新验证 console 无应用错误。

## 覆盖路径

- 平台管理员读取 `/sync` 状态摘要。
- 平台管理员运行飞书通讯录权限预检，并查看阶段结果 Drawer。
- scheduled run 在详情 Drawer 中显示 `系统任务`。
- `sync:view` 用户可读同步记录，缺少 `sync:run` 时无法手动同步、运行预检或重试。
- 后端 scheduled sync 使用 `operator_type='system'` 和 `operator_feishu_user_id=null`，不伪造飞书用户。

## 验证命令

```bash
npm test -- src/pages/Sync/index.test.tsx
npm run build
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/sync.test.ts server/tests/migrations.test.ts
npm run server:build
git diff --check
```

## 结果摘要

- Frontend targeted tests：4 passed。
- Frontend build：passed，保留既有 chunk size warning。
- Server targeted tests：14 passed。
- Server build：passed。
- Browser verification：passed，console 仅剩 React DevTools 开发提示；停止 dev server 后出现 Vite polling 日志，与应用行为无关。
- `git diff --check`：passed。
