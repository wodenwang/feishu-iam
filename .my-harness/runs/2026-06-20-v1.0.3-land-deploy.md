# v1.0.3 land-and-deploy

## 目标

完成 `v1.0.3 - Base Portal 接入包与完整提示词收敛` 的远端部署和生产健康验证。

## 输入状态

- GitHub Release：`https://github.com/wodenwang/feishu-iam/releases/tag/v1.0.3`。
- release commit：`53f94b01d65eb8fb77f76eddcb8e545bcb4f39db`。
- 本地工作区仍保留并行域名清理未提交改动；构建使用干净 `v1.0.3` worktree，避免污染部署镜像。

## 镜像

- 构建命令：`docker buildx build --platform linux/amd64 -f deploy/api.Dockerfile -t feishu-iam:v1.0.3 --load .`
- 本地镜像：`feishu-iam:v1.0.3`。
- 平台：`linux/amd64`。
- manifest digest：`sha256:fad6c05532a12fbc4c83abc83e00a6d606eb222978e912c304a05e5740622c1c`。
- 远端加载：`docker save feishu-iam:v1.0.3 | gzip -1 | ssh ... 'gzip -dc | docker load'`。

## 远端升级

- 服务器：`bpmt@120.24.236.92`。
- 部署目录：`/home/bpmt/feishu-iam`。
- `.env` 备份：`backups/env-before-v1.0.3-20260620-104059`。
- 版本键：
  - `FEISHU_IAM_IMAGE=feishu-iam`
  - `FEISHU_IAM_IMAGE_TAG=v1.0.3`
  - `APP_VERSION=1.0.3`
  - `GIT_COMMIT=53f94b0`
  - `FEISHU_IAM_PULL_POLICY=never`
- 升级命令：`cd /home/bpmt/feishu-iam && ./upgrade.sh`。
- 升级备份目录：`/home/bpmt/feishu-iam/backups/20260620-104106`。

## 生产验证

- 容器：`feishu-iam-web-1 feishu-iam:v1.0.3 Up`，端口 `8002->3000`。
- 本机 `http://127.0.0.1:8002/ready` 返回 ready。
- 本机 `http://127.0.0.1:8002/version` 返回 `1.0.3 / 53f94b0`。
- 公网 `https://feishu-iam.riversoft.com.cn/ready` 返回 ready。
- 公网 `https://feishu-iam.riversoft.com.cn/version` 返回 `1.0.3 / 53f94b0`。
- 日志确认 `POST /api/v1/admin/applications/:appKey/integration-prompt/refresh` 路由已加载，Nest application successfully started。

## 结论

`v1.0.3` 第 15 步 `gstack /land-and-deploy` 已完成。未记录任何明文 secret、token、cookie、密码或 `.env` 敏感值。
