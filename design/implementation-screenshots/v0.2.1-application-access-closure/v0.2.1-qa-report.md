# v0.2.1 应用接入闭环补缺 QA 报告

日期：2026-05-27

## 结论

通过。角色绑定权限组、第三方应用接入提示词复制、未登录跳转提示和版本文档均已完成本地验证。

## 自动化验证

| 命令 | 结果 |
|---|---|
| `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test` | 通过，19 files / 96 tests |
| `npm test` | 通过，25 files / 128 tests |
| `npm run server:build` | 通过 |
| `npm run build` | 通过，保留 Vite chunk size 警告 |
| `docker build -t feishu-iam:v0.2.1-local .` | 通过，保留 Vite chunk size 警告 |
| `git diff --check` | 通过 |

## 浏览器验证

运行环境：

- Frontend：`http://127.0.0.1:5173`
- 数据模式：Mock data
- Viewports：1440x900、768x1024

覆盖路径：

- `/login?status=loginRequired`：展示“需要通过飞书登录”，无 username/password 入口。
- `/applications/app_demo_crm`：接入配置 Tab 展示“第三方应用接入提示词”和“复制应用提示词”按钮。
- `/applications/app_demo_crm`：点击复制按钮后展示“应用提示词已复制，并记录审计事件”。
- `/roles`：角色列表权限数量展示为“权限组 / 权限点”。
- `/roles`：配置授权 Drawer 内权限组父节点可直接勾选或取消，组织和用户选择仍保留。

## 截图证据

- `v0.2.1-login-required-desktop.png`
- `v0.2.1-login-required-tablet.png`
- `v0.2.1-application-prompt-desktop.png`
- `v0.2.1-roles-permission-group-drawer.png`

## 不回归边界

- 未新增 username/password、本地 root 或非飞书登录体系。
- 未在提示词中明文输出真实 `appSecret` / `apiSecret`。
- 未扩大到完整 SDK、CLI、OIDC/JWKS/refresh token 或多租户 IdP。
- 未重开 `/directory` 独立组织编辑范围。
