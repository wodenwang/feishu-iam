# v0.1.16 同步运营 Runtime Ship 收口记录

## 范围

当前 ship 收口只覆盖同步运营 Runtime diff：

- `GET /api/sync/status`
- `POST /api/sync/preflight`
- scheduled full sync
- `sync_runs` system actor
- `/sync` 页面健康状态、预检 Drawer 和 scheduled/system actor 可见性
- 对应计划、设计、QA、review 和截图证据

明确不包含：

- tag / GitHub Release
- deploy / land-and-deploy
- `/directory` 扩展

## 验证

```bash
npm test -- src/pages/Sync/index.test.tsx
npm run build
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/sync.test.ts server/tests/migrations.test.ts
npm run server:build
git diff --check
```

结果：

- frontend targeted test：4 passed
- frontend build：passed，保留既有 chunk size warning
- server targeted tests：14 passed
- server build：passed
- diff check：passed
- release metadata：已更新 `README.md`、`CHANGELOG.md`、`VERSION`、`package.json` 和 `package-lock.json` 到 `v0.1.16`

## Commit message

```text
feat: add sync operations runtime
```

## PR title

```text
feat: add v0.1.16 sync operations runtime
```

## PR body

```markdown
## Summary

- add sync health and directory preflight runtime APIs
- add scheduled full sync with explicit system actor support
- surface sync health, preflight results, and system scheduled runs on `/sync`
- bump release metadata to v0.1.16

## Scope boundaries

- does not fake a Feishu user for scheduled/system runs
- does not extend `/directory`
- does not add tag, GitHub Release, or deployment changes
- does not persist Feishu app secrets, tenant tokens, raw directory payloads, exported user lists, or sync snapshots

## Verification

- `npm test -- src/pages/Sync/index.test.tsx`
- `npm run build`
- `TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/feishu_iam_test npm run server:test -- server/tests/sync.test.ts server/tests/migrations.test.ts`
- `npm run server:build`
- `git diff --check`

## Evidence

- `design/implementation-screenshots/v0.1.16-sync-ops-runtime/feishu-iam-v0.1.16-sync-preflight.png`
- `design/implementation-screenshots/v0.1.16-sync-ops-runtime/v0.1.16-design-review.md`
- `design/implementation-screenshots/v0.1.16-sync-ops-runtime/v0.1.16-qa-report.md`
- `design/implementation-screenshots/v0.1.16-sync-ops-runtime/v0.1.16-review-report.md`
```
