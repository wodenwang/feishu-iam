# feishu-iam v0.1.5 Roles HTTP Implementation Plan

当前执行计划见仓库根目录 `IMPLEMENTATION_PLAN.md`。

本文件用于满足 `docs/superpowers/plans/` 的计划归档约定。`IMPLEMENTATION_PLAN.md` 是本切片的可执行计划，覆盖：

- `/roles` HTTP mode 角色列表。
- 角色创建、编辑、停用。
- 授权保存。
- permission tree runtime projection。
- 401 / 403 / 409 / API error / requestId。
- 后端 roles contract、前端 DTO mapping、HTTP service、页面状态和 E2E 验证。

非目标：

- 扩大 `/directory` 只读浏览边界。
- Sync。
- Dashboard。
- Application Detail。
- Onboarding。
- 真实飞书 OAuth。
- deploy、release、tag。
