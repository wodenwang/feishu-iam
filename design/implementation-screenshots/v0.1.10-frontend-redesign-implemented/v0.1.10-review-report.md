# v0.1.10 Admin Console 前端重构落地前 review

日期：2026-05-24

## 结论

通过。当前 diff 与 v0.1.10 前端重构范围一致，没有发现阻塞落地的问题。

## review 循环指标

- review 循环：1 次。
- 发现问题：1 个。
- 已修复：1 个。
- 遗留：0 个。

## 重点检查

- Feishu-only 登录边界：登录页没有 username / password，真实入口仍为飞书 OAuth，本地 mock 登录仅在开发 HTTP 模式显示。
- 退出登录：新增 `POST /api/auth/logout`，删除当前 session token hash 并清理 `iam_session` cookie；未引入独立认证系统。
- Admin Shell：使用 Ant Design `Layout / Menu / Breadcrumb` 模式，响应式折叠 Sider，Header 右侧聚合环境和飞书用户菜单。
- 权限树：多应用重复 permission code 时后端去重，避免 React Tree duplicate key。
- 测试：单元、服务端、构建和 Playwright 三视口链路均通过。

## 风险记录

- 前端生产构建仍有 Vite chunk size warning，属于既有打包体积问题，不阻塞本次 UI slice。
- `jsdom` 下 Ant Design 仍输出 `getComputedStyle` pseudo-elements warning，测试退出码为 0。
