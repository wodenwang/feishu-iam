# Feishu IAM v0.15.0 组织树与组织用户选择器预合入代码评审

评审时间：2026-05-28 17:03

## 结论

无阻塞或重要代码问题，可以进入 Git closeout、ship、land-and-deploy。

## 检查点

- API 契约：应用级飞书部门/用户候选接口补齐分页、父级/部门过滤和状态字段；角色主体保存支持 `org_subjects` / `user_subjects`，并兼容 legacy `subjects`。
- 保存语义：前端保存时按组织主体和用户主体拆分 payload；服务层继续使用扁平主体输入并批量判断用户、部门是否存在，保留 orphaned 语义。
- 区域隔离失败：`OrgBrowser` 错误态只替换当前浏览区域，不清空右侧详情、已选草稿或外部排障内容。
- 响应式：角色绑定窄屏使用 `Tabs` 分步，不再依赖旧 `min-w-[760px]` 横向滚动作为主流程。
- #24：角色管理操作列固定宽度，危险停用动作有确认，按钮具备 `aria-label` 和 `title`。
- 文档与版本：`README.md`、`CHANGELOG.md`、部署默认值和 package 版本已切到 v0.15.0；镜像 digest 和部署证据待发布后回填。

## 遗留

- 当前无必须修复遗留。
- #21 仍明确排除到后续小版本。
