# 2026-06-06 22:08 v1.0.0 Riversoft 本地 ship 收口

## 会话目标

使用 gstack `/ship` 对 Feishu IAM v1.0.0 Riversoft 正式版第一个 vertical slice 做本地 Git 和发布材料收口。

## 用户关键要求摘要

- 确认提交边界只包含 v1.0.0 Riversoft UI 翻新、数据初始化、Logo 修正、QA 修复、设计/验收/会话归档相关产物。
- 复核 diff 中没有真实 secret、token、cookie、飞书 app_secret、数据库密码、生产导出数据或无关临时文件。
- 复核 v1.0.0 初始化迁移只确保“王文哲”获得 IAM 平台管理员权限。
- 运行 ship 阶段必要验证命令。
- 准备提交信息、版本/README/验收材料和发布说明。
- 未经明确授权，不 push、不创建 PR、不打 tag、不发布镜像、不 merge、不 release、不 deploy。

## 本轮关键操作

- 阅读 gstack `/ship` 与 my-harness Codex-safe gstack gate 规则。
- 复核工作区 WIP、版本号、迁移边界和敏感信息扫描结果。
- 补充 `CHANGELOG.md` 的 `v1.0.0` 本地收口发布说明，只记录已发生的本地验证，不声称已发布或已部署。
- 启动本地 admin-web dev server，完成响应式矩阵和 390px 移动导航定点复测。
- 关闭本地 Vite dev server，避免留下后台进程。

## 修改文件

- `CHANGELOG.md`
- `docs/codex-sessions/2026-06-06-2208-v1.0.0-riversoft-ship-closeout.md`

本轮之前已存在的 v1.0.0 产物仍保持在当前工作区，包括前端 UI、Logo、迁移、设计、验收和 QA 修复文件。

## 验证结果

- `pnpm check`：通过。
  - API：41 个测试文件、460 个测试通过。
  - Admin Web：15 个测试文件、156 个测试通过。
  - typecheck 与 lint 均通过。
- `pnpm --filter @feishu-iam/admin-web test:buttons`：通过，`button governance check passed`。
- `ADMIN_WEB_URL=http://localhost:5173 pnpm --filter @feishu-iam/admin-web test:responsive`：通过，覆盖 13 条后台路由和 390、768、1280、1440 视口，结果 `failures: []`。
- 390px Playwright 移动导航定点复测：通过。打开移动导航后点击 `应用管理`，跳转到 `/admin/applications`，Sheet 自动关闭，页面级 `scrollWidth` 等于 `viewportWidth`。
- `git diff --check`：通过。

## 安全与边界复核

- 敏感信息扫描未发现真实 `sk-`、secret、token、cookie、飞书 app_secret、数据库密码或生产导出数据。
- 命中项为占位符、测试假值、既有安全文档或脱敏测试。
- `migrations/V1_0_0__platform_admin_initialization.sql` 只写入 `admin_users`、`admin_user_roles`、`audit_logs` 和 `schema_versions`。
- 未初始化第三方应用权限、demo client、回调地址、权限点、权限组或业务角色，也不撤销其他管理员。

## 未执行事项

- 未 push。
- 未创建 PR。
- 未打 tag。
- 未发布镜像。
- 未 merge、release 或 deploy。

## 下一步建议

进入 `gstack /land-and-deploy` 前，应先由用户明确授权远端动作，包括 push、PR、tag、镜像构建/上传、release、部署和线上健康检查。
