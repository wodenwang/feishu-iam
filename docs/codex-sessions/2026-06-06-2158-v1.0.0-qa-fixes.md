# 2026-06-06 21:58 v1.0.0 Riversoft QA 必修项修复

## 会话目标

修复 Feishu IAM v1.0.0 Riversoft 正式版第一个 vertical slice 的 gstack /qa 必须修复项，并在修复后重新执行指定 QA 验证。

## 用户关键要求摘要

- 只修复 QA-1 和 QA-2，不扩大范围。
- QA-1：`apps/admin-web/src/App.test.tsx` 仍断言旧品牌文案和旧 logo label，需要同步为 Riversoft 文案。
- QA-2：390px 移动端 Sheet 导航点击 `应用管理` 后背景跳转但 Sheet 未关闭，需要点击导航入口后关闭菜单。
- 不改变 UX 流程、信息架构、路由、表单流程、数据契约、CRUD 行为、权限逻辑或业务逻辑。
- 不提交真实 secret、token、cookie、飞书 app_secret、数据库密码或生产导出数据。

## 关键约束和决策

- QA-1 仅更新测试断言，不改变生产文案来源。
- QA-2 仅将移动端 Sheet 变为受控打开状态，并在移动 Sheet 内导航链接点击后关闭 Sheet。
- 桌面 Sidebar 不传入 `onNavigate`，因此桌面导航行为不变。
- 系统管理分组展开按钮不触发 Sheet 关闭，只有实际导航入口点击后关闭。
- 本轮曾发现两处非必要样式漂移，已还原，最终 diff 收窄到 QA 必修项。

## 修改文件

- `apps/admin-web/src/App.test.tsx`
- `apps/admin-web/src/components/admin/AppShell.tsx`

## 验证结果

- `pnpm --filter @feishu-iam/admin-web typecheck`：通过。
- `pnpm --filter @feishu-iam/admin-web lint`：通过。
- `pnpm --filter @feishu-iam/admin-web test`：通过，15 个测试文件、156 个测试通过。
- `pnpm --filter @feishu-iam/admin-web test:buttons`：通过，`button governance check passed`。
- `ADMIN_WEB_URL=http://localhost:5173 pnpm --filter @feishu-iam/admin-web test:responsive`：通过，覆盖 390、768、1280、1440 视口和关键后台路由，结果 `failures: []`。
- 390px Playwright 移动导航定点复测：通过。打开移动导航后点击 `应用管理`，URL 跳转到 `/admin/applications`，Sheet 关闭，页面级 `scrollWidth` 等于 `viewportWidth`，无 console error 和非预期 request failure。

## 敏感信息检查

本轮没有新增 secret、token、cookie、飞书 app_secret、数据库密码或生产导出数据。

## 未完成事项和下一步建议

- 可以进入 `gstack /review`，重点审查本次 QA 修复 diff 是否严格收窄，以及整个 v1.0.0 vertical slice 的数据、权限和安全边界是否可落地。
