# v0.3.1 部署记录

## 发布对象

- 版本：`v0.3.1`
- 发布主题：Admin Shell 外框与顶部栏修复
- 发布提交：`ced7e0919b82b4d9591fe2ffdf5a59563ca59af2`
- Pull Request：`https://github.com/wodenwang/feishu-iam/pull/26`
- PR GitHub Actions：`https://github.com/wodenwang/feishu-iam/actions/runs/26536819113`，结论 `success`
- main GitHub Actions：`https://github.com/wodenwang/feishu-iam/actions/runs/26537080671`，结论 `success`
- GitHub Release：`https://github.com/wodenwang/feishu-iam/releases/tag/v0.3.1`
- 部署时间：`2026-05-28 04:40 CST`

## 部署环境

- 目标主机：`bpmt-120`
- 目标目录：`/home/bpmt/feishu-iam`
- Compose 服务：`feishu-iam`、`postgres`
- 应用镜像：`feishu-iam:v0.3.1`
- 镜像 ID：`sha256:4126b8d6e10793f102e5de783df2570386c5b4b5ed8841b59a49811439a37e1a`
- 对外端口：`8002 -> 4100`
- 公网入口：`https://feishu-iam.riversoft.com.cn`

## 部署动作

1. 使用 `git archive --format=tar HEAD` 将仓库受控文件同步到远端部署目录。
2. 保留远端 `.env` 与 `data/postgres`，并创建 `.env.backup-v0.3.1-*`。
3. 将远端 `.env` 中的 `FEISHU_IAM_IMAGE_TAG` 更新为 `v0.3.1`。
4. 在远端执行 `docker compose up -d --build --remove-orphans` 完成镜像构建、容器重建和启动。

## 验证证据

- 远端 Compose 状态：

```text
feishu-iam            feishu-iam:v0.3.1    Up (healthy)    0.0.0.0:8002->4100/tcp
feishu-iam-postgres   postgres:16-alpine   Up (healthy)    5432/tcp
```

- 远端本机健康检查：

```json
{"ok":true}
```

- 公网健康检查：

```json
{"ok":true}
```

- 公网登录页：

```text
GET /login HTTP 200，返回 Vite HTML。
```

## 边界确认

- 本版本只修复 Admin Shell 外框、顶部栏、账户入口和侧边栏收缩交互。
- 系统仍保持飞书唯一身份源，未新增 username/password、本地 root 或独立账号体系。
- 远端 `.env`、飞书应用凭证、tokens、同步快照和 PostgreSQL 数据目录未纳入仓库同步。
